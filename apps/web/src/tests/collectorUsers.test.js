import{describe,it,expect}from'vitest';import{readFileSync}from'node:fs';import{join}from'node:path';
describe('collector user management',()=>{it('provides type-scoped tabs, account edit and password reset',()=>{const page=readFileSync(join(process.cwd(),'src/pages/admin/system/UsersPage.vue'),'utf8'),api=readFileSync(join(process.cwd(),'src/lib/userApi.js'),'utf8');expect(page).toContain('Web 端账号');expect(page).toContain('小程序端账号');expect(page).toContain("userType:view.value");expect(page).not.toContain('#actions');expect(page).toContain('修改账号');expect(page).toContain('重置密码');expect(api).toContain('/collector-account')});it('uses prefixed user id and unified loginName without legacy internal numbers',()=>{const page=readFileSync(join(process.cwd(),'src/pages/admin/system/UsersPage.vue'),'utf8');expect(page).toContain('row.id');expect(page).toContain('row.loginName');expect(page).toContain('row.userType');expect(page).not.toContain('internalUserNo');expect(page).not.toContain('内部编号')})})
